Track meaningful work across the agent run, reset before each run. Test text-only final turns and subsequent empty runs. Use RPC select with the full proposal; fall back when terminal UI is unavailable. Preserve explicit terminal decisions. Never infer approval from an absent dialog on UI-capable hosts. Preserve the existing non-UI policy. Release through existing tag-triggered CI/OIDC publication.
