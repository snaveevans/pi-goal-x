# Assessment log

- 2026-09-22: Used `gh repo view`, full open/all-state issue and PR lists, PR metadata/diffs/comments/reviews, inline review comments, workflow runs and a pinned main archive. Inventory: 27 issues total (6 open), 52 PRs total (5 open). Limits of 500 covered the complete returned issue/PR inventories.
- Read project instructions, README/package/CI, reliability and prior resolution specifications, and the unfinished local budget recovery PRODUCT/TECH/MILESTONES. Local main is `5a7c4cd` (0.31.5); remote main is `8b077aa6a1e9c83709bf639af79528501f8273d0` (0.31.6). Preserved all existing changes and used a temporary archive for remote code assessment.
- Reviewed all five open PR diffs and associated discussion. All were reported mergeable but unstable; corresponding CI runs reported `action_required`. Contributor-reported suites were not treated as independently verified. No review/inline discussion supplied additional conclusions at these open heads.
- Read relevant closed issue/PR history. In particular, #64 restores implicit continuation after #58, and maintainer comments on #60/#61 explicitly decline full Windows test-suite portability. #70 is closed without comments/reviews explaining the decision; its incidents are used as investigation leads, not accepted requirements.
- Applied #79's diff to a disposable copy of pinned main. The first absolute-path `git apply` attempt was rejected because macOS `/tmp` is a symbolic-link path; applying from inside the disposable copy succeeded. No workspace source files were changed.
- Ran #79's `goal-live-retention` and `goal-prompt-cache` test files with Node v26.7.0: 23 passed, zero failures/skips. These targeted results do not validate the entire PR or the supported Node/SDK matrix.
- Two additional offline probes confirmed that #79 still misses a trailing effort-only message and that its retention layer preserves an old budget line when a subsequent counters block omits it. The latter is a deterministic retention example, not a demonstrated model decision or full end-to-end budget-recovery failure. Recorded code and outputs in EVIDENCE.md.
- Wrote PRODUCT.md with complete per-item necessity/disposition, sequencing and history-guided preventive work; TECH.md with implementation contracts and merge/release gates. No production fixes, branch updates, GitHub comments, issue closures, merges or releases were performed.

## Integration

- User requested full implementation as one PR, incorporating applicable open contributions, with no new commands and tight runtime/context costs. Updated PRODUCT/TECH before implementation; created a managed worktree from remote main and preserved all original uncommitted work.
- Integrated #66/#75/#79 and adapted #71. Imported the existing budget recovery work instead of #74's extra command/tool. Added the scoped shared-root resolver, stale-widget cleanup, lifetime/context labels and narrow historical auditor safeguards.
- Retention review found removed limits could remain in replayed counters. Moved limits into policy identity, removed redundant live metadata, capped retained counter bytes and removed a redundant unbounded tracking map. Added combined marker/retention and advancing real-SDK regressions.
- Initial storage implementation added settings/filesystem operations to low-level hot paths. Restored the original cwd-only contract and default-path I/O behavior; isolated main/integration runs now count exactly the same 5,896 filesystem operations. External pools retain explicit validation and session pinning.
- SDK inventory advanced to 0.87.0 during implementation. Normalization required by the newer Responses adapter initially broke the direct serialization harness; normalized before calling it. Pi 0.83 rejected a newer test-only option in typecheck; made that option structurally compatible and reran successfully.
- One benchmark run was interrupted while dependency installation changed its SDK tree. Reran from stable installs. B10 and a goal-creation operation baseline were stale relative to remote main; corrected their reference assumptions without relaxing regression thresholds.
- Full suite, supported-host boundary checks, actual Node floor, provider construction, context, performance, audit and packed-loader smoke pass. Details and limits are in VALIDATION.md. No live provider call or package publication was made.

- First GitHub run passed all host matrices, full tests, context/provider/retention checks and packaging/audit, but the newly active runtime gate compared Linux timings against historical local 10× targets (0.7/1.1 ms versus 0.3/0.6 ms). Added a same-runner base/candidate comparison using the existing regression tolerances; historical campaign headroom checks remain unchanged for local runs. Missing benchmark outputs now fail rather than silently succeeding.
