# Budget visibility and recovery (#59)

Use the existing /goal-tweak conversation and confirmation to set or remove the total lifetime token budget. Add no commands, tools or settings. Budget-only requests retain objective, task evidence, mode and auditor configuration. Show old/proposed budgets before confirmation and saved effective budgets afterwards; all creation reports show Budget: none or Budget: N tokens.

Optional propose_goal_draft token_budget: positive safe integer sets; null removes; omission retains a tweak budget or leaves a new goal unlimited. Never reinterpret positive budgets. Confirmed recovery resumes only through existing scheduler ownership, interruption and allowance safeguards, without resetting usage or run allowance. Still-exhausted goals stay budget_limited. Lowered exhausted limits stop active goals immediately. Cancelled, stale and failed proposals do not change goals.

Coalesce threshold warnings. Document Responses serialization separately: local Pi 0.84.1 omits strict unless compat.supportsStrictMode is true; no independent live OpenCode reproduction is claimed. Provide boundary tests and a per-model workaround, not global rewrites or relaxed argument validation.
