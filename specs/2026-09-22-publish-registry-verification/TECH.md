# Implementation

Extend the existing bounded polling loops from 12 to 60 attempts at five-second intervals and report progress. Download the ranking artifact by the successful ranking job's artifact ID output rather than recomputing its name with a later run-attempt number. Exercise the embedded publish script using mocked registry responses, sleeps and publication: propagation beyond one minute, existing identical package, mismatch, backwards latest and timeout.
